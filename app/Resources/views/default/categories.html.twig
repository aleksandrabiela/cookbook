{% extends 'base.html.twig' %}

{% block body %}
    <br/> <br/>
    <a href="/category" class="btn btn-primary">Dodaj kategorię</a> <br/><br/><br/>
    {% for category in Categories %}
        <div class="well" style="text-align:center; max-width:300px; margin:10px auto">  
            <h4> {{ category.name }} <br/> </h4>
            {{ category.description }} <br/>
             <a href="/category/{{ category.id }}" class="btn btn-primary">Edytuj</a>
        </div>
    {% endfor %}
{% endblock %}